# rag-flask-mongo

README must include
1. Architecture diagram (can paste the one above)
2. One-command quickstart: docker compose up
3. Example curl requests for /ingest and /query
4. Design decisions section — why ChromaDB, why sentence-transformers, why overlapping chunks. This is what signals senior-level thinking to recruiters.
5. "What I'd add next" — streaming, auth, multi-tenancy, async ingestion queue. Shows you think about production.